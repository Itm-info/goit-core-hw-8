package itm.project.filter;

import itm.project.shape.Shape;

public class Flip extends Filter {
    public Flip(String id, Shape shape, Axis axis) {
        super(id);
        /*filter effect code here*/
    }
}
